# Pre-Finalize UI Checklist

- All visible rider and driver strings use `data-i18n*`.
- New keys exist in both locale files.
- No hardcoded `aria-label` values without translation keys.
- CSS uses tokens except approved rgba or token-derived gradients.
- Layout still works in Arabic RTL and English LTR.
